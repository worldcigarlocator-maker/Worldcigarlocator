name: 🔍 Test Supabase Secrets

on:
  workflow_dispatch:  # gör att du kan köra manuellt

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Print confirmation (inte nycklarna)
        run: |
          echo "✅ SUPABASE_URL is set: ${{ secrets.SUPABASE_URL != '' }}"
          echo "✅ SERVICE_ROLE_KEY is set: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' }}"
          echo "✅ ANON_KEY is set: ${{ secrets.SUPABASE_ANON_KEY != '' }}"
          echo "✅ GOOGLE_MAPS_API_KEY is set: ${{ secrets.GOOGLE_MAPS_API_KEY != '' }}"
